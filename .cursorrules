# 月子小家 · 全局开发规则

## 一、项目简介

这是一个**家庭内部使用的月子生活信息小程序**，名称：月子小家。
面向家人使用，用于查看月子期间的饮食建议、妈妈照护重点、宝宝护理要点和家庭分工安排。
风格：温柔、治愈、简洁、母婴感强，绝不电商风，绝不医院后台风。

---

## 二、技术栈

### 前端
- 框架：UniApp（基于 Vue 3 Composition API）
- 语言：JavaScript（不使用 TypeScript）
- 样式：WXSS + UnoCSS（或原生 scoped style）
- 组件库：uview-plus（u-前缀组件）
- 状态管理：Pinia
- 请求封装：统一封装 `uni.request`，放在 `utils/request.js`

### 后端
- 运行时：Node.js
- 框架：Koa2
- 语言：JavaScript（不使用 TypeScript）
- 数据库：MySQL（第一版可用本地 JSON mock 数据）
- 接口风格：RESTful API

---

## 三、目录结构规范

### 前端目录（UniApp）
```
paipai-mini/
  pages/              # 页面，每个页面一个文件夹
    index/            # 首页
    meals/            # 月子餐
    mother-care/      # 妈妈照护
    baby-care/        # 宝宝护理
    family-plan/      # 家庭安排
    knowledge/        # 月子知识
  components/         # 公共组件
    common/           # 通用组件（SectionTitle、Tag、EmptyState 等）
    cards/            # 卡片组件（MealCard、CareCard、PlanCard 等）
    layout/           # 布局组件（PageHeader、PageFooter 等）
  store/              # Pinia 状态管理
  utils/              # 工具函数
    request.js        # 请求封装
    date.js           # 日期工具
    format.js         # 格式化工具
  data/               # 本地 mock 数据（JSON 或 JS）
    site.js
    meals.js
    motherCare.js
    babyCare.js
    familyPlan.js
    knowledge.js
  static/             # 静态资源（图片、图标）
  styles/             # 全局样式
    variables.wxss    # CSS 变量/主题色
    common.wxss       # 公共样式
  App.vue
  main.js
  pages.json
  manifest.json
```

### 后端目录（Koa）
```
server/
  src/
    routes/           # 路由注册
    controllers/      # 业务控制器
    services/         # 服务层（数据处理）
    middleware/       # 中间件（日志、鉴权、错误处理）
    data/             # mock JSON 数据（第一版）
    utils/            # 工具函数
    app.js            # Koa 入口
  package.json
  .env                # 环境变量
```

---

## 四、代码风格规范

### 通用
- 使用 2 空格缩进
- 字符串优先使用单引号 `'`
- 每个文件末尾保留一个空行
- 禁止出现 `console.log`（调试完毕后清除）
- 注释使用中文

### Vue 组件规范
- 使用 `<script setup>` 语法
- 组件名使用 PascalCase，如 `MealCard.vue`
- props 必须声明类型和默认值
- 事件名使用 kebab-case，如 `@click-item`
- 模板中禁止写复杂逻辑，提取到 computed 或方法中

```vue
<!-- 正确示例 -->
<script setup>
// 引入
import { ref, computed } from 'vue'

// props
const props = defineProps({
  title: { type: String, default: '' },
  list: { type: Array, default: () => [] }
})

// emits
const emit = defineEmits(['click-item'])

// 数据
const activeTab = ref(0)

// 计算属性
const filteredList = computed(() => props.list.filter(i => i.active))
</script>
```

### 样式规范
- 所有页面使用 `scoped` 样式
- 颜色统一使用 CSS 变量，不允许硬编码色值
- 不允许使用 `!important`
- 类名使用 kebab-case，如 `.meal-card__title`

---

## 五、主题色规范

```css
/* styles/variables.wxss */
:root {
  --color-bg: #FFFAF5;           /* 奶白背景 */
  --color-surface: #FFFFFF;      /* 卡片白 */
  --color-primary: #E8A598;      /* 浅粉主色 */
  --color-secondary: #C8D8C0;    /* 鼠尾草绿 */
  --color-accent: #F2DFC8;       /* 浅杏色 */
  --color-text-primary: #3D3532; /* 主文字深棕 */
  --color-text-secondary: #8C7B72; /* 次文字暖灰 */
  --color-text-hint: #B5A89F;    /* 提示文字 */
  --color-border: #EDE0D8;       /* 边框色 */
  --radius-sm: 8rpx;
  --radius-md: 16rpx;
  --radius-lg: 24rpx;
  --radius-xl: 32rpx;
  --shadow-card: 0 2rpx 16rpx rgba(200, 150, 130, 0.1);
}
```

---

## 六、组件规范

### 必须抽离的公共组件
| 组件名 | 说明 |
|--------|------|
| `SectionTitle` | 区块标题（带左侧装饰线） |
| `InfoCard` | 通用信息卡片 |
| `MealCard` | 月子餐菜品卡片 |
| `CareCard` | 照护建议卡片 |
| `PlanCard` | 家庭安排卡片 |
| `Tag` | 功效/分类标签 |
| `Tabs` | Tab 切换组件 |
| `Accordion` | 折叠展开（FAQ 用） |
| `EmptyState` | 空状态组件 |
| `PageLayout` | 页面容器（统一内边距） |

### 组件 props 命名规范
- 数据类：`list`、`item`、`data`
- 状态类：`loading`、`disabled`、`active`
- 配置类：`title`、`label`、`placeholder`
- 事件类：`onXxx`（不用，统一用 emit）

---

## 七、接口规范

### 前端请求
- 所有请求必须走 `utils/request.js` 封装
- 接口地址统一配置在 `utils/request.js` 的 `BASE_URL`
- 请求方法统一返回 Promise

```js
// utils/request.js 示例
const BASE_URL = 'http://localhost:3000/api'

export const request = (options) => {
  return new Promise((resolve, reject) => {
    uni.request({
      url: BASE_URL + options.url,
      method: options.method || 'GET',
      data: options.data || {},
      success: (res) => {
        if (res.data.code === 0) {
          resolve(res.data.data)
        } else {
          uni.showToast({ title: res.data.message || '请求失败', icon: 'none' })
          reject(res.data)
        }
      },
      fail: reject
    })
  })
}
```

### 后端接口响应格式
```js
// 统一返回格式
{
  code: 0,          // 0=成功，非0=错误
  message: '成功',
  data: {}          // 实际数据
}
```

### 接口路由命名（RESTful）
```
GET    /api/meals              # 获取月子餐列表
GET    /api/meals/:id          # 获取单个菜品
GET    /api/mother-care        # 获取妈妈照护信息
GET    /api/baby-care          # 获取宝宝护理信息
GET    /api/family-plan        # 获取家庭安排
GET    /api/knowledge          # 获取月子知识列表
```

---

## 八、数据规范（mock 数据结构）

```js
// data/meals.js 示例
export const mealsData = [
  {
    id: 1,
    week: 1,           // 第几周
    mealType: '早餐',   // 早餐/加餐/午餐/晚餐/汤水
    name: '小米粥',
    desc: '健脾养胃，适合产后第一周',
    tags: ['开胃', '易消化'],
    avoid: '不宜加糖过多'
  }
]
```

---

## 九、禁止事项

- ❌ 禁止页面中出现英文 UI 文案（所有展示文字必须为简体中文）
- ❌ 禁止硬编码颜色值（统一用 CSS 变量）
- ❌ 禁止在 template 中写复杂逻辑
- ❌ 禁止跳过组件抽离直接在页面堆代码
- ❌ 禁止使用 `any` 类型（如果后续引入 TS）
- ❌ 禁止接口直接写在页面组件里（统一在 `api/` 目录）
- ❌ 禁止提交含有 `console.log` 的代码

---

## 十、Git 提交规范

```
feat: 新增月子餐页面
fix: 修复首页今日餐单不显示的问题
style: 调整卡片圆角和间距
refactor: 重构请求封装工具
docs: 更新开发文档
chore: 更新依赖版本
```

---

## 十一、开发顺序建议

1. 初始化 UniApp 项目
2. 配置全局样式变量
3. 实现公共组件
4. 实现首页（含所有区块）
5. 实现月子餐页面
6. 实现妈妈照护页面
7. 实现宝宝护理页面
8. 实现家庭安排页面
9. 实现月子知识页面
10. 联调后端接口（替换 mock 数据）
11. 真机测试与调优
12. 上线部署
